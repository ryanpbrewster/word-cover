import styled from 'styled-components';

export const BigButton = styled.button`
  height: 48px;
  width: 96px;
  border-radius: 16px;
  margin: 24px;
  font-size: 32px;

  &:focus {
    outline: none;
  }
`;

